public class TwoDArrays {
    public static void main(String[] args) {
        // Rows, Colums
        double[][] a = new double[100][3];

        for (int r = 0; a.length; r++) {
            for (int c = 0; c < a[r].length; c++) {
                scores[r][c] = Math.random() * 10;
            }
        }

        // Average

        // Too make rows with different colums
        int[][] b = new int[2][];

        b[0] = new int[5];
        b[1] = new int[3];

    }
}